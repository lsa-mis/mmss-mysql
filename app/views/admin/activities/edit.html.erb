<% admin_page_title "Edit Activity ##{@activity.id}", subtitle: @activity.description %>
<% admin_page_actions do %>
  <%= link_to 'View activity', admin_activity_path(@activity), class: 'admin-btn-secondary' %>
<% end %>

<%= render 'admin/activities/form', activity: @activity %>
