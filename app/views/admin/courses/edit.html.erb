<% admin_page_title "Edit Course ##{@course.id}", subtitle: @course.title %>
<% admin_page_actions do %>
  <%= link_to 'View course', admin_course_path(@course), class: 'admin-btn-secondary' %>
<% end %>

<%= render 'admin/courses/form', course: @course %>
